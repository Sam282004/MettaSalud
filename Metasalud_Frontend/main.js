/*jQuery('document').ready(function($) {

    var btn = $('.menu-icon'),
        menu = $('.nav ul');
    btn.click(function() {

        if (menu.hasClass('mostrar')) {

            menu.removeClass('mostrar');

        } else {
            menu.addClass('mostrar')
        }
    })
});*/